spring:
  application:
    name: ktm-rest
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://docker.for.mac.localhost:8888}
#      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
#      failFast: true
  profiles:
    active: "dev"